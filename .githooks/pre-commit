#!/bin/sh
set -e

# 1. Format and fix staged files with Biome, re-staging changes
pnpm biome check --write --staged --no-errors-on-unmatched
git update-index --again

# 2. Verify production build succeeds
pnpm build
